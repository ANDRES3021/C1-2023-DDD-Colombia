 /**
 *Verifica si un número es mayor que cero.
 *
 * @param {number} value
 * @return {*}  {boolean}
 */
 export const isGreaterZero = (value: number): boolean => {
  if (value > 0)
    return true;
  return false;
};
  